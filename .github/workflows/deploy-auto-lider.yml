name: Deploy Autolider

on:
  push:
    branches:
      - prod/auto-lider

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.SFTP_SERVER_AUTO_LIDER }}
          username: ${{ secrets.SFTP_USER_AUTO_LIDER }}
          password: ${{ secrets.SFTP_PASSWORD_AUTO_LIDER }}
          protocol: ftps
          port: 21
          security: loose
          server-dir: /
          local-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
